apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-deployment-#{Release.Artifacts.image.BuildId}#
  labels:
    app: web
    type: "#{deploy}#"
spec:
  replicas: 3
  selector:
    matchLabels:
      app: web
      version: "web-#{Release.Artifacts.image.BuildId}#"
      type: "#{deploy}#"
  template:
    metadata:
      labels:
        app: web
        version: "web-#{Release.Artifacts.image.BuildId}#"
        type: "#{deploy}#"
    spec:
      containers: 
        - name: web
          image: dpolania/blue-green
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 80