apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-deployment-#{Release.Artifacts.image.BuildId}#
  labels:
    version: "#{deploy}#"
spec:
  replicas: 3
  selector:
    matchLabels:
      app: web
  template:
    metadata:
      labels:
        app: web
        version: "#{deploy}#"
    spec:
      containers: 
        - name: web
          image: dpolania/blue-green
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 80